# Updated requirements.txt for ITS4.3
biopython>=1.79
mdtraj>=1.9.7
scipy>=1.7.0
scikit-learn>=1.0.0
numpy>=1.21.0
pandas>=1.3.0
matplotlib>=3.5.0
seaborn>=0.11.0

# New dependencies for ITS4.2
cobra>=0.27.0
torch>=1.12.0
transformers>=4.20.0
scanpy>=1.9.0
anndata>=0.8.0
h5py>=3.7.0
requests>=2.28.0
tqdm>=4.64.0

# UI and visualization
streamlit>=1.22.0

# CADD Pathogenicity Scoring Dependencies
# These are additional dependencies for the CADD pathogenicity scorer
# Most are already included above, but explicitly listed for clarity

# Optional ML model dependencies
# tensorflow>=2.9.0  # For Enformer if using TF implementation
# pytorch-lightning>=1.6.0  # For some models

# External tools (need to be installed separately)
# fpocket: Protein pocket detection tool - installed via WSL Ubuntu
# Installation: git clone https://github.com/Discngine/fpocket.git && cd fpocket && make && sudo make install